#include <iostream>
#include <windows.h>
#include <cmath>
int localize() {
    SetConsoleCP(1251);
    SetConsoleOutputCP(1251);
    int task8();
    return 0;
}
//int main(){
//    unsigned char variable = 0;
//    do {
//        std::cout << (int)variable << std::endl;
//        ++variable;
//    } while (variable != 0);
//
//    return 0;
//}

//int main() {
//    localize();
//    int a, b, c;
//    float x1, x2, D;
//    std::cout << "Введите коэффициенты квадратного уравнения по одному через enter (a, b и c)"
//    std::cin >> a;
//    std::cin >> b;
//    std::cin >> c;
//    D = (b * b) - (4 * a * c);
//    if (D == 0) {
//        x1 = (float)(-b / (2 * a));
//        std::cout << "Один корень: x = " << x1;
//    }
//    else if (D > 0) {
//        x1 = (float)((-b - sqrt(D)) / (2 * a));
//        x2 = (float)((-b + sqrt(D)) / (2 * a));
//        std::cout << "Два корня: x1 = " << x1 << "\t x2 = " << x2 << std::endl;
//    }
//    else {
//        std::cout << "Нет корней";
//    }
//}

//int main(){
//    localize();
//    std::cout << "Введите координаты точки через enter (х, затем у)" << std::endl;
//    int x, y;
//    int r, R;
//    std::cin >> x;
//    std::cin >> y;
//    std::cout << "Введите радиусы окружностей через enter (Радиус малой, затем большой)" << std::endl;
//    std::cin >> r;
//    std::cin >> R;
//    if (x * x + y * y > r * r and x * x + y * y < R * R) {
//        std::cout << "YES" << std::endl;
//    }
//    else {
//        std::cout << "NO" << std::endl;
//    }
//}

//int main() {
//    localize();
//    float x = 0, eps;
//    float rez = 0, counter = 0;
//    std::cout << "Введите х от -1 до 1" << std::endl;
//    std::cin >> x;
//    std::cout << "Введите значение eps" << std::endl;
//    std::cin >> eps;
//    while (abs(pow(x, counter)) > eps) {
//        ++counter;
//        rez += pow(x, counter);
//    }
//    std::cout << "Результат работы программы: " << rez << std::endl;
//    std::cout << "Вычисленное значение по формуле 1/(1-x) равно " << 1. / (1. - x) << "\n" << "Разница в значениях составляет " << abs(rez - (1. / (1. - x))) << std::endl;
//}

//int main() {
//    int a, b, c, d;
//    float x, y;
//    return 0;
//}

//int main()
//{
//    int counter = 0;
//    for (int i = 0; i < 100; ++i) 
//    {
//        ++counter;
//        if (counter % 3 == 0)
//        {
//            if (counter % 5 == 0)
//            {
//                std::cout << "FooBar" << std::endl;
//            }
//            else
//            {
//                std::cout << "Foo" << std::endl;
//            }
//        }
//        else if (counter % 5 == 0)
//            {
//                std::cout << "Bar" << std::endl;
//        }
//        else {
//            std::cout << counter << std::endl;
//        }
//    }
//    return 0;
//}

