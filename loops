#include<iostream> 

int main() {
    setlocale(LC_ALL, "ru");
    int num;
    float sum = 0;
    std::cout << "Введите число n = " << std::endl; std::cin >> num;
    for (int i = 1; i <= num; i++){
        sum += 1. / i;
    }
    std::cout << "Ответ: " << sum << std::endl;

    return 0;
}
